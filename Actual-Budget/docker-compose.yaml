services:
  actual_server:
    image: ghcr.io/actualbudget/actual:latest
    container_name: actual_budget
    ports:
      - 5006:5006
    volumes:
      - ./actual-data:/data
    restart: unless-stopped